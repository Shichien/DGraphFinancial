#import "template/preamble.typ": template

#show: template.with(
  header: [DGCheater 金融反诈智能守护赛道一方案报告],
  author: "参赛团队",
  title: "金融反诈智能守护赛道一方案报告",
)

= 项目定位

本文档面向金融反诈智能守护赛题的方向一 欺诈交易智能识别任务，给出当前作品的技术方案、实验结果、工程落地设计与赛题对照说明。

本项目的交付重点不是泛化到所有反诈方向，而是围绕方向一要求，完成一条可复现、可解释、可扩展的欺诈交易识别主链路。当前已经落地的核心能力包括：

- 基于真实大规模图交易数据的离线识别主模型
- 多维图结构与时间统计特征工程
- 无测试标签泄漏的训练与验证流程
- 面向答辩展示的匿名关系面板与推理闭环可视化
- 面向风控复核的调查控制台、案件处置状态、审计记录与一跳溯源视图
- 可复现实验记录、提交文件生成与技术报告
- 面向在线识别场景的系统架构设计与性能测试方案

= 赛道一要求拆解

赛题规则对方向一的关键要求，可以拆成三层：

- 第一层是必须讲清楚的识别主链路，包括数据表示、特征来源、模型设计、风险输出与效果评估
- 第二层是完成度加分项，包括仿真交易流、流式处理、自适应学习与模型迭代
- 第三层是工程化展示项，包括系统架构、技术选型、性能测试与安全合规说明

需要特别说明的是，以下内容并不是方向一的必做核心：

- 方向二中的虚假信息、钓鱼 URL、多模态内容识别
- 方向三中的反诈客服、对话系统、知识库问答
- 把图神经网络作为唯一主方案
- 在初版作品中就完整实现混合云生产部署

方向一更看重的是，是否围绕欺诈交易识别做出一套逻辑闭环、技术可信、结果可解释的方案。设备指纹、社交关联、图神经网络、混合云部署都属于鼓励项，不是缺一不可的硬门槛。

== 当前完成情况

#table(
  columns: (1.8fr, 1.2fr, 3.2fr),
  [赛题要求], [当前状态], [说明],
  [多维度特征构建], [已完成], [已经实现原始节点特征、交易类型统计、时间统计、邻居统计、背景节点上下文与无泄漏风险邻域特征],
  [高准确率与低误报率], [已完成], [主数据 `phase1_gdata.npz` 上当前验证 AUC 为 `0.832586`，明显强于公开 baseline],
  [团伙结构识别能力], [部分完成], [当前模型已经利用图结构与风险邻域信息捕捉团伙信号，并新增高风险事件一跳邻域溯源摘要，但还没有完整的交互式社区发现产品模块],
  [仿真环境], [部分完成], [已接入 `AMLSim sample`，并基于本地 DGraph-Fin 交易边实现按时间戳回放的交易流仿真原型；展示面板支持生成一次仿真场景],
  [实时识别与动态异常检测], [原型完成], [已新增单机交易流回放与微批评分原型，可输出风险分、风险等级、风控动作与解释字段],
  [案件处置与审计记录], [已完成], [展示面板已读取流式风险事件，形成案件列表、处理状态、复核结论、风险解释和可审计预测记录],
  [流式数据处理框架], [原型完成], [已新增 Kafka、Flink、风险评分服务、producer 与 consumer 的 Docker Compose 部署包；当前机器未安装 Docker，已完成静态验证],
  [自适应学习与模型迭代], [部分完成], [已具备多数据集扩展与实验迭代机制，尚未实现在线回灌式增量学习],
  [可解释可视化展示], [已完成], [已生成答辩用前端面板，包含可信成绩墙、匿名关系子图、推理主链路、仿真剧本、模型识别结果、阈值命中量、调查控制台与完成度看板],
  [系统架构图与技术选型], [已完成], [报告中已给出模块拆分、技术选型与部署建议],
  [性能测试方案], [已完成], [已完成单机交易流回放实测，并保留离线耗时、在线延迟、吞吐与稳定性测试方案],
  [安全合规说明], [已完成], [已给出数据最小化、隔离、审计与部署安全的说明],
)

= 当前最容易漏讲的五个得分点

当前项目里，有五类内容其实已经具备明显得分潜力，但如果只讲模型分数，反而容易被弱化：

1. 可信性不是口头承诺，而是主动排雷能力
   - `DGraph-Fin2` 中，项目已经实际识别并移除了会直接暴露正类身份的节点时间标签
   - `IEEE-CIS` 中，项目已经主动把验证口径从随机切分改为时间切分
   - `EllipticPlusPlus` 中，项目进一步识别并修复了时间字段直入特征、伪造边时间戳和验证口径回退等问题

2. 主成绩提升链条非常完整
   - 项目不是只给出一个最终 AUC，而是清楚展示了从原始节点特征，到图结构增强，再到无泄漏风险邻域增强与融合模型的逐步增益过程

3. 统一架构能力已经被多数据集验证
   - 当前框架已覆盖图节点反欺诈、时间增强图、表格交易欺诈和 AML actor 图，不是只对一个文件写出来的脚本

4. 可解释展示已经形成闭环
   - 当前已经补齐匿名关系子图、特征重要度、推理链路说明、仿真欺诈剧本、场景生成、模型识别结果、案件处置和调查控制台，适合答辩现场直接展示

5. 工程可复现性已经明显强于只交模型文件的方案
   - 当前仓库已经具备 `uv` 环境管理、统一 CLI、实验记录、报告生成和答辩面板生成能力

= 数据与问题定义

== 主数据

当前主实验基于 `phase1_gdata.npz` 开展，其特点如下：

- 节点数约 402 万
- 边数约 492 万
- 节点原始特征维度 17
- 边属性包含交易类型与时间戳
- 训练节点索引约 82.7 万
- 测试节点索引约 35.5 万

训练标签是二分类反欺诈任务，其中 0 表示正常样本，1 表示欺诈样本。图中还存在标签值 2 与 3 的背景节点，它们不参与主评估标签，但可以为图结构特征提供上下文。

== 辅助公开数据

为了验证方案的可迁移性与工程扩展能力，当前工程还完成了以下公开数据集接入：

- `DGraph-Fin`
- `DGraph-Fin2`
- `IEEE-CIS Fraud Detection`
- `EllipticPlusPlus`
- `AMLSim sample`

这些数据集不直接替代赛题主数据，但可以帮助说明本方案不是只对单一文件生效的临时脚本，而是一套可以迁移的统一架构。

= 主方案设计

== 设计原则

本项目当前阶段优先遵循四条原则：

- 优先保证主数据上的识别效果和可复现性
- 严格避免测试标签或伪标签泄漏
- 优先选择训练稳定、推理高效、解释性较强的模型
- 把生产级 Kafka/Flink 部署、混合云部署与图神经网络增强放在第二阶段扩展

因此，本次主提交方案选择树模型为核心，而不是先把复杂图神经网络系统作为第一交付物。

== 特征工程

当前主方案构建了以下几类特征：

- 原始节点属性特征
- 缺失值统计特征
- 入度、出度、总度及其对数变换
- 各交易类型的入边、出边与总量计数
- 各交易类型覆盖度统计
- 交易时间均值、最早时间、最晚时间与跨度
- 背景标签 2、3 邻居数量与占比
- 原始 17 维特征的一跳邻居最大值、最小值与均值统计
- 基于训练折已知标签构造的无泄漏风险邻域统计

这些特征已经覆盖了赛题方向一中最重要的交易网络、时间行为、结构上下文和多维度特征融合要求。

当前没有显式接入设备指纹与用户社交关系字段，原因不是方向错误，而是赛题主数据本身没有直接提供这两类字段。对于答辩，可以明确说明：

- 当前已实现的是交易网络与时间行为画像
- 设备指纹与社交关联属于后续可接入的业务增强维度
- 方案的特征框架已经为这些新增字段预留了自然扩展空间

== 模型设计

主模型采用 `XGBoost` 与 `LightGBM` 的轻量融合策略，其中 `XGBoost` 作为主干，`LightGBM` 作为辅助模型。最终输出按 `0.9 * XGBoost + 0.1 * LightGBM` 融合。

选择这一方案的原因有三点：

- 对稀疏统计特征与非线性关系建模稳定
- 训练成本相对可控，适合快速迭代
- 特征重要度容易解释，便于人工研判和答辩展示

同时，模型训练显式考虑类别不平衡问题，并使用只依赖训练折已知标签的风险邻域增强，保证增强过程不触碰验证集与测试集标签。

== 验证口径

为了避免虚高结果，当前工程对不同公开数据集采用了更严格的可信验证口径：

- `phase1_gdata.npz` 使用主数据既定训练索引，并从训练集中划分验证集
- `DGraph-Fin2` 已剔除会直接泄漏正类身份的节点时间标签
- `IEEE-CIS` 已从随机分层切分改为基于 `TransactionDT` 的时间切分
- `EllipticPlusPlus` 已进一步移除 `Time step` 直入特征、停用伪造边时间戳，并直接用未来时间段做验证

这一点非常重要，因为比赛更重视可信的技术闭环，而不是表面上更高但解释不住的离线分数。

= 实验结果

== 主数据结果

当前在赛题主数据 `phase1_gdata.npz` 上，本地验证结果如下：

- 原始 17 维特征与 `LightGBM`，验证 AUC 约为 `0.7387`
- 图结构增强特征与 `LightGBM`，验证 AUC 约为 `0.8218`
- 图结构增强特征、无泄漏风险邻域增强与 `XGBoost`，验证 AUC 约为 `0.8325`
- 图结构增强特征、无泄漏风险邻域增强以及树模型融合，验证 AUC 约为 `0.832586`

这说明当前主方案的收益主要来自两部分：

- 图结构与时间统计特征显著提升了可分性
- 无泄漏风险邻域增强进一步强化了结构风险传播信号

== 主数据增益链

#table(
  columns: (2.2fr, 1.2fr, 2.6fr),
  [阶段], [验证 AUC], [意义],
  [原始 17 维节点特征 + `LightGBM`], [`0.7387`], [说明单纯节点属性对复杂欺诈团伙识别能力有限],
  [图结构与时间统计特征 + `LightGBM`], [`0.8218`], [说明图结构、交易方向和时间行为是赛道一最关键的增益来源],
  [图结构增强 + 无泄漏风险邻域增强 + 融合模型], [`0.832586`], [说明在不触碰测试标签前提下，结构风险传播信息还可以继续提升主成绩],
)

这张增益链很适合作为答辩中的效果证明，因为它展示的是清晰的工程演进，而不是一个孤立的最终分数。

== 公开数据辅助验证

#table(
  columns: (1.6fr, 1.2fr, 3.0fr),
  [数据集], [可信 AUC], [说明],
  [`DGraph-Fin`], [`0.828120`], [与主数据最接近，验证当前图特征与树模型方案具有迁移性],
  [`DGraph-Fin2`], [`0.827919`], [去除节点时间标签泄漏后结果稳定，说明方案对时间增强图仍然有效],
  [`IEEE-CIS`], [`0.914582`], [改用时间切分后回落到更可信区间，说明先前高分主要来自验证口径偏乐观],
  [`EllipticPlusPlus`], [`0.926556`], [修正时间字段与验证口径后已显著回落，说明旧的近满分记录主要被实现口径问题抬高；但该数据仍更像全生命周期 actor 分类补充集，而不是严格在线因果时序基准],
)

这些结果的意义在于补充说明方案具有扩展性，但比赛答辩时不应把公开数据上的绝对 AUC 直接当成主成绩展示。主成绩仍应以 `phase1_gdata.npz` 为核心。

== 与公开方案对比

本项目吸收并核查了以下公开实现：

- `DGraphXinye/DGraphFin_baseline`
- `ytchx1999/TGN-DGraphFin`
- `qiyue111-hub/DgraphFin_anti_fraud`
- `DGraphXinye/2022_finvcup_baseline`
- `RobertAckleyKid/2022_finvcup_RobertAckley_8363`
- `storyandwine/GEARSage-DGraphFin`

当前主方案在主数据上的验证 AUC 达到 `0.832586`，已经显著高于官方 baseline 仓库中公开的 `GraphSAGE` 邻居采样基线。

对于公开仓库中的更高离线分数，本项目采取的态度是：

- 可以吸收不会引起泄漏的特征工程与结构建模思路
- 不直接搬用依赖不同数据版本、测试标签或不一致评测口径的离线成绩
- 优先保证当前仓库结果可复现、可解释、可答辩

具体落到当前作品中，公开方案给了三类有效启发。第一类是官方基线和 GraphSAGE 系列，它们证明邻居采样和图结构确实是 DGraphFin 的基础能力，因此本项目保留了交易方向、交易类型、一跳邻居和风险邻域统计。第二类是 GEARSage 和 TGN 这类动态图方案，它们强调边时间、动态邻居和时间事件建模，因此本项目在离线特征里加入时间窗口统计，并在展示侧新增交易流回放和风险事件输出。第三类是风控调查台类项目，它们提醒作品不能只停留在分数，而要让评委看到风险等级、处置动作、复核结论和审计链路，因此当前大屏新增公开方案对照、仿真欺诈剧本、场景生成、调查控制台、模型版本、阈值策略和一跳溯源视图。

这里的取舍是有意控制复杂度：不直接引入需要 A100 级显存和长时间预训练的动态图模型作为第一交付物，也不把其他仓库在不同数据或不同评估流程下的高分写成当前主成绩。当前版本先把可运行、可复核、可展示的闭环做扎实，再把动态图模型、社区发现和策略回灌作为后续增强项。

= 系统落地设计

== 系统边界

对赛道一来说，当前最合理的答辩策略不是声称已经完整实现生产系统，而是明确区分：

- 已经完成的离线识别主链路
- 已经完成的单机交易流回放与在线评分原型
- 已经完成部署包的 Kafka 与 Flink 流式扩展架构
- 计划在下一阶段补齐的生产级流式服务、压测看板与交互式图溯源模块

这种表达更稳妥，也更符合当前实际完成度。

== 架构设计

当前建议的系统结构如下：

- 数据层：交易节点属性、交易边、交易类型、时间戳与已知风险标签
- 特征层：维护度数、交易类型统计、时间统计与风险邻域特征
- 模型层：离线训练融合模型，在线加载模型做风险评分
- 风控层：输出风险分数、风险等级、关键特征与疑似关联链路
- 运维层：记录模型版本、评分日志、特征快照与人工复核结果

如需做赛题原型系统展示，可采用以下技术路线：

1. Kafka 接入交易流
2. Flink 维护增量状态与窗口统计
3. Python 推理服务完成实时评分
4. 图检索或社区发现模块做链路溯源
5. 风控台完成告警展示、人工复核与策略回灌

当前仓库已经先落地了一个轻量原型：基于 DGraph-Fin 交易边按时间戳抽样生成交易流，补充渠道、金额与设备指纹模拟字段，加载已训练的 `XGBoost + LightGBM` 模型进行微批评分，并输出风险等级与高风险邻域溯源摘要。在此基础上，仓库还新增了 `deploy/streaming/docker-compose.yml`，包含 Kafka、Kafka UI、Flink JobManager、Flink TaskManager、PyFlink 风险作业、HTTP 风险评分服务、Kafka producer 与结果 consumer。当前机器命令行未检测到 Docker，因此本轮完成的是可启动部署包与静态验证，实际端到端启动需要 Docker Desktop 或兼容 Docker Engine。

== 关键技术选型

#table(
  columns: (1.4fr, 1.2fr, 3.2fr),
  [模块], [技术选型], [选择理由],
  [图数据处理], [NumPy / Pandas], [与当前数据格式天然兼容，适合大规模离线特征构建],
  [特征缓存], [Joblib], [缓存大规模特征矩阵与模型文件，复现实验方便],
  [主模型], [XGBoost], [对统计类特征建模稳定，推理速度快，适合作为主提交模型],
  [辅助模型], [LightGBM], [训练成本低，与主模型形成互补，提高稳定性],
  [环境管理], [uv], [依赖和锁文件清晰，便于在不同环境复现实验],
  [文档与报告], [Typst], [适合版本化维护比赛报告，并快速同步实验更新],
  [流式扩展], [Kafka 与 Flink], [已提供 Docker Compose 部署包，符合赛题对交易流识别和动态异常检测的工程化预期],
)

== 性能测试方案

当前已经具备完整的性能测试设计方案，可在答辩时作为工程成熟度说明：

- 离线训练耗时：统计特征构建时间、训练时间与提交文件生成时间
- 在线单笔延迟：模拟单条交易事件进入后的特征更新与评分耗时
- 批量吞吐能力：回放仿真交易流，观察平均延迟与峰值延迟
- 稳定性：长时间运行下监控状态更新和日志输出是否稳定
- 模型迭代能力：补充新欺诈样本后重新训练，比较 AUC 与特征变化

当前还补充了单机回放实测。基于 `DGraph-Fin` 抽样回放 `5000` 条交易事件时，唯一评分节点数为 `9986`，总运行时间约 `0.1130` 秒，模型评分时间约 `0.0784` 秒，事件吞吐约 `44231.03` events/second，纯评分吞吐约 `127411.78` nodes/second，平均节点评分延迟约 `0.0078 ms`。这组数据来自 `output/streaming/performance_report.md`，口径是单机 CSV 回放与微批评分，不等同于完整 Kafka/Flink 端到端压测。

在展示层面，当前面板已经把风险事件进一步组织成调查控制台。左侧是按风险分排序的案件列表，右侧展示焦点节点、风险分、建议动作、处理状态、复核结论、风险解释、审计记录和一跳邻域结构。这样答辩时可以从单条交易风险跳转到团伙溯源证据，而不是只展示静态统计摘要。

仿真环境部分也补充了四类可解释欺诈剧本：循环转账、分散汇集、集中转出和账户团伙。每个剧本都明确说明交易链路、关键风险信号和模型响应方式，用于支撑赛题对多样性、隐蔽性及团伙协作欺诈场景的要求。展示面板还提供生成一次仿真场景的入口，能够现场展示交易链路和模型识别结果，避免剧本只停留在静态说明。

= 安全性与可解释性

赛题评分中单独给了安全性，因此答辩时不能只讲模型分数。当前可交付的安全与解释性内容包括：

- 数据最小化使用原则与脱敏存储
- 模型文件与特征缓存分级隔离
- 审计日志记录每次训练与评分请求
- 通过树模型特征重要度支撑人工复核
- 对高风险样本输出关键驱动特征与关联结构证据
- 对实时回放事件输出 `low`、`medium`、`high`、`critical` 风险等级与对应风控动作
- 展示模型版本、训练数据、AUC、阈值策略、各等级命中量、批处理输入输出文件和可审计预测记录

当前特征重要度结果显示，时间统计、关键交易类型计数、图结构度数与风险邻域特征具有较高贡献，这与反诈业务中时间异常、关系异常和交易类型异常的直觉一致。

此外，当前仓库已经补齐了答辩用前端展示面板，可直接把可信成绩墙、匿名关系子图、仿真欺诈剧本、案件调查视图、模型识别结果、阈值命中量、推理链路和完成度看板一起展示出来。这意味着当前项目的可解释性不再只停留在表格和文字层面，而是已经形成可视化证据闭环。

= 当前交付物

截至当前版本，仓库已经实际完成：

- `uv` 管理的 Python 项目骨架
- 基于真实大规模图数据的统一特征工程实现
- 融合模型训练入口与提交文件生成
- 公开数据集统一注册表与扩展框架
- `DGraph-Fin`、`DGraph-Fin2`、`IEEE-CIS`、`EllipticPlusPlus`、`AMLSim sample` 接入验证
- 特征重要度导出与实验记录文档
- 答辩用前端展示面板与匿名关系子图
- 仿真欺诈剧本、阈值策略、案件处置和调查控制台展示
- 单机交易流回放、在线评分、风险等级输出、团伙溯源摘要与性能实测报告
- Typst 报告模板与 PDF 产物
- 外部公开参考实现归档与核查

尚未完全落地但已经完成设计的部分包括：

- 多源异构仿真交易流系统
- Kafka 与 Flink 部署包已完成，生产级安全认证和端到端云上压测待补齐
- 团伙链路可视化与交互式溯源模块
- 混合云部署原型与端到端压测图表

= 答辩建议

赛道一答辩时，建议把重点放在以下四件事上：

1. 先讲问题难点：跨时段、跨渠道、结构化团伙协作让传统单点规则不够用
2. 再讲我们已经做成的主链路：图结构特征、时间统计、风险邻域增强、融合模型
3. 展示主数据结果：从 `0.7387` 提升到 `0.832586`
4. 最后讲工程化扩展：仿真环境、流式处理、在线部署与性能测试方案

如果现场时间有限，最需要守住的三个判断是：

- 当前作品已经完成赛道一的识别核心链路
- 当前结果在主数据上可信，没有依赖测试标签泄漏
- 当前工程已经为仿真环境、流式识别和系统部署预留了清晰的升级路线

= 下一步计划

下一阶段优先补强以下内容：

- 基于 `AMLSim` 或 `IBM AML` 构建更完整的仿真交易流演示
- 增加团伙检测与风险链路可视化
- 补充离线训练耗时与在线模拟延迟实测数据
- 生成答辩 PPT 与演示脚本
- 视资源情况增加时序图模型作为增强方案，而不是替换当前主方案
