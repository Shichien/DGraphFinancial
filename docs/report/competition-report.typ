#import "template/preamble.typ": template

#show: template.with(
  header: [DGCheater 金融反诈智能守护技术报告],
  author: "参赛团队",
  title: "DGCheater 金融反诈智能守护技术报告",
)

#align(center)[
  #text(size: 18pt, weight: "bold")[DGCheater 金融反诈智能守护技术报告]

  #v(0.8em)
  #text(size: 11pt)[方向一：欺诈交易智能识别]
]

#outline(title: [目录])

= 摘要

本项目面向金融反诈智能守护赛道方向一，围绕第三方支付和跨渠道交易风险识别，构建了一套从公开图数据建模、多源仿真交易流生成、实时风险评分、图关系追溯到可视化大屏的完整原型系统。系统以 DGraph-Fin 图交易数据为离线识别主基准，结合交易网络、时间行为、交易类型、风险邻域和多源仿真字段形成风险特征；离线侧采用树模型融合完成欺诈节点识别，在线侧通过 Kafka 主题、多源特征汇合、风险评分服务、PostgreSQL、Redis、Neo4j 和 Vue 三维大屏形成实时识别闭环。

当前稳定交付链路为 Kafka 多源仿真输入、Python 实时特征后端、评分消费者、风险事件入库和大屏展示。Flink 版本已经实现多源状态化特征作业和本地状态逻辑验证，并纳入 Docker Compose 部署包；在提交版本中作为流式增强后端保留。系统输出风险分、风险等级、处置建议、原因码、团伙编号、相关节点、审计记录和案件处置状态，满足赛题对仿真环境、实时识别、动态异常检测、风险等级输出、可解释溯源和系统架构说明的核心要求。

= 赛题理解与目标拆解

方向一要求参赛团队自建或模拟包含多源异构金融交易数据的仿真环境，支持跨时段、跨渠道交易行为生成，涵盖正常交易与具有多样性、隐蔽性和团伙协作性的欺诈交易场景。系统需要在交易发生时完成风险评估和欺诈判定，并输出识别结果与风险等级。赛题还鼓励融合行为画像、设备指纹、交易网络和用户社交关联等多维特征，适配流式数据处理框架，支持可解释溯源、模型迭代和混合云部署设计。

基于以上要求，本项目将目标拆成六个可验收模块：

- 离线识别主模型：在公开图交易数据上形成可复现的欺诈识别能力。
- 多源仿真环境：生成交易、账户画像、设备登录、黑名单和延迟标签等事件。
- 实时处理链路：交易进入消息队列后完成特征汇合、评分、入库和告警。
- 图追溯能力：风险事件能返回真实相关节点、关系边和团伙编号。
- 可视化大屏：以评委可直接观察的方式展示实时进件、风险等级、告警队列、点云关系图和审计记录。
- 工程交付能力：提供一键启动命令、统一命令行、部署文档、性能记录和技术报告。

== 赛题要求对照

#table(
  columns: (1.4fr, 1.1fr, 3.4fr),
  [要求], [状态], [对应实现],
  [多源异构仿真环境], [已完成], [仿真器生成交易、账户画像、设备登录、黑名单和延迟标签，覆盖银行转账、钱包支付、商户收单、二维码支付和移动银行等渠道。],
  [正常交易与欺诈交易场景], [已完成], [已注入分散汇入、试探后转移、循环转账、设备复用、IP 聚集、商户洗钱、跨渠道规避和突发交易等剧本。],
  [实时识别与动态异常检测], [已完成], [Kafka 输入后由实时特征后端和评分消费者完成风险判定，并将结果写入风险主题、数据库和大屏接口。],
  [风险等级输出], [已完成], [输出低危、中危、高危、严重四档风险等级，并给出放行、二次验证、人工复核、冻结拦截等处置建议。],
  [多维特征构建], [已完成], [融合原始节点特征、交易类型、时间统计、图邻域、账户画像、设备、IP、商户和黑名单命中特征。],
  [流式处理框架], [已实现], [提供 Kafka 本地链路、Docker Compose 基础设施、Flink 多源状态作业和 Python 稳定特征后端。],
  [团伙结构识别与溯源], [已完成], [动态图状态维护一跳邻居、二跳组件、风险邻居数和团伙编号，风险事件写入关联边并支持大屏点击追溯。],
  [自适应学习与模型迭代], [部分完成], [已形成离线再训练、统一数据注册和延迟标签回流接口；自动增量训练和灰度发布作为后续增强。],
  [架构图与技术选型], [已完成], [报告、文档和部署说明均给出模块拆分、技术栈和启动方式。],
  [性能测试方案], [已完成], [提供单机回放性能记录、端到端验收脚本和实时指标文件。],
)

= 数据与问题定义

== DGraph-Fin 图交易数据

离线主任务采用 DGraph-Fin 作为核心公开基准。该数据集包含约 370 万个匿名节点和约 430 万条交易边，节点原始特征维度为 17，边属性包含交易类型和时间戳。训练标签中 0 表示正常节点，1 表示欺诈节点，背景节点用于提供图上下文但不直接参与二分类目标。

本项目将 DGraph-Fin 定义为图节点风险识别任务：输入为匿名账户节点特征、交易边、边类型、边时间以及训练集标签；输出为测试节点的欺诈概率，在线系统中进一步映射为风险等级、原因码和处置动作。这个定义与金融账户风险识别场景一致，也能自然扩展到交易流在线评分。

== 多源仿真交易流

仅依赖 DGraph-Fin 原始字段无法覆盖赛题要求中的设备指纹、跨渠道交易和黑名单变化。因此项目在不引入个人敏感信息的前提下，构造了仿真交易流事件：

- 交易事件：事件编号、时间戳、源账户、目标账户、金额、渠道、交易类型、商户和设备标识。
- 账户画像：账户年龄、历史风险、常用渠道、常用商户和历史行为摘要。
- 设备登录：设备编号、IP、地理区域、是否触发登录挑战和设备账户数量。
- 黑名单事件：账户、设备、IP 或商户维度的风险命中。
- 延迟标签：用于后续模型迭代和效果复核，不参与在线评分主流程。

在线评分阶段不读取仿真标签字段。欺诈剧本标签只用于烟测评估和可解释展示，避免将答案字段泄漏进评分逻辑。

== 辅助公开数据

为验证方案可迁移性，项目还接入并复核了 DGraph-Fin2、IEEE-CIS、EllipticPlusPlus 和 AMLSim sample。不同数据集的字段、时间口径和标签定义不同，因此报告只将其作为架构复用和泄漏检查的辅助证据，不将指标做简单横向排名。

= 总体架构

#figure(
  image("../online-deployment/figures/realtime-antifraud-platform-preview-1.png", width: 100%),
  caption: [实时反诈平台架构图],
)

系统采用离线建模和在线识别双链路设计。离线侧负责训练稳定模型、生成特征缓存、输出提交文件和评估报告；在线侧负责接收仿真交易流、构建实时特征、完成风险评分、写入风险事件库并刷新可视化大屏。

在线链路如下：

```text
多源仿真器
-> Kafka 原始主题
-> 实时特征后端
-> features.realtime
-> 风险评分消费者
-> risk.scored / risk.alerts / risk.audit
-> PostgreSQL / Redis / Neo4j
-> Vue 三维实时大屏
```

其中 Kafka 承载多源事件流，PostgreSQL 存储风险事件、原因、关联边、审计日志和案件处置记录，Redis 维护高风险节点榜和近期告警，Neo4j 保存账户、设备、IP、商户之间的异构图关系，Vue 大屏负责实时展示和人工复核。

== 技术选型

#table(
  columns: (1.3fr, 1.4fr, 3.2fr),
  [模块], [技术选型], [说明],
  [离线特征], [NumPy、Pandas], [适合处理大规模图统计、时间统计和表格特征。],
  [主模型], [XGBoost、LightGBM], [对稀疏统计特征稳定，训练成本可控，便于解释和快速迭代。],
  [消息队列], [Kafka], [承载交易、账户、设备、黑名单和风险判定等事件主题。],
  [实时特征], [Python 后端、Flink 作业], [Python 后端作为稳定演示链路，Flink 作业作为状态化流处理增强实现。],
  [服务接口], [FastAPI], [提供大屏数据、节点邻域、审计日志、案件处置和节点特征查询。],
  [关系存储], [Neo4j], [保存账户、设备、IP、商户的异构关系，用于团伙追溯。],
  [事件存储], [PostgreSQL、Redis], [分别承担可审计持久化和实时榜单缓存。],
  [前端展示], [Vue、Canvas 三维点云], [展示实时进件、风险等级、告警队列、团伙子图和解释面板。],
  [工程管理], [uv、Just、Docker Compose], [统一依赖、启动命令和本地基础设施。],
)

= 离线识别模型

== 特征工程

离线特征围绕图结构、时间行为和风险邻域构建，主要包括：

- 原始 17 维节点属性和缺失值统计。
- 入度、出度、总度及其对数变换。
- 各交易类型的入边、出边和总量计数。
- 交易类型覆盖度、交易时间均值、最早时间、最晚时间和时间跨度。
- 背景节点邻居数量和占比。
- 一跳邻居原始特征的最大值、最小值和均值。
- 只依赖训练折已知标签的风险邻域统计。

对于有时间属性的数据集，聚合统计遵循时间因果约束，避免使用未来信息。对于图数据，风险邻域增强只使用训练折可见标签，避免把验证或测试答案注入特征。

== 模型结构

主模型采用 XGBoost 与 LightGBM 融合。DGraph-Fin 主提交使用 0.9 倍 XGBoost 和 0.1 倍 LightGBM 的加权融合。选择树模型作为主方案有三个原因：

- 能稳定处理稀疏统计特征和非线性组合关系。
- 推理速度快，适合在线风险评分。
- 特征重要度和分裂路径能支撑人工复核解释。

在线评分器直接加载 DGraph-Fin 训练得到的 XGBoost 与 LightGBM 模型，将源账户和目标账户映射到 DGraph 节点空间，分别计算 126 维图特征模型先验，并取两端账户较高分作为交易账户风险先验。在此基础上，再引入实时行为分、图团伙分和规则分，综合风险分为：

```text
风险分 = 0.45 * DGraph-Fin 账户模型先验
      + 0.30 * 实时行为风险
      + 0.15 * 图团伙风险
      + 0.10 * 规则命中风险
```

该权重已经通过评分契约烟测校验，保证前端解释面板和后端评分逻辑使用同一套口径。证据字段中会保留 DGraph 源账户分、目标账户分、节点映射、模型特征维度和 DGraph 验证 AUC，用于证明大屏中的模型先验来自 DGraph-Fin 公开图模型，而不是规则分伪装。

== 泄漏控制

反欺诈数据很容易因时间字段、身份字段或标签邻居造成虚高结果。本项目在多个公开数据集上显式做了泄漏检查：

- DGraph-Fin2 中剔除可能直接暴露正类身份的节点时间标签。
- IEEE-CIS 中从随机切分改为按交易时间切分。
- EllipticPlusPlus 中移除直接时间字段和不符合因果口径的伪造边时间戳。
- 图邻域标签统计只在训练折内计算，验证和测试不读取未来标签。

因此报告中的指标优先采用可信验证口径，而不是保留看似更高但解释风险更大的结果。

= 实时识别系统

== 多源仿真器

仿真器以匿名账户和交易关系为基础，按时间生成多源事件，并注入不同类型的欺诈剧本。当前支持的主要剧本包括：

- 分散汇入后集中转出：多个账户向同一账户汇入后快速转移。
- 试探后转移：先产生小额低频试探，再发起大额转账。
- 循环转账：多个账户形成闭环资金流。
- 设备复用：多个账户共享同一设备或相似登录环境。
- IP 聚集：异常账户集中出现在同一 IP 或区域。
- 商户洗钱：资金通过高风险商户或商户簇流转。
- 跨渠道规避：同一账户在短时间内切换多个渠道。
- 突发交易：历史低活跃账户突然产生高频交易。

这些剧本覆盖多样性、隐蔽性和团伙协作性三类赛题重点。仿真事件同时包含正常交易，使系统能够在同一条实时流中比较正常行为和异常行为。

== 实时特征后端

实时特征后端维护账户、设备、IP、商户和图邻域状态。它会按 event_id 汇合交易、账户画像、设备登录和可选黑名单事件，满足必要来源后再输出 features.realtime。这样可以避免 Kafka 多主题到达顺序不一致导致账户画像、设备挑战或黑名单证据缺失。

当前稳定演示链路使用 Python 实时特征后端。Flink 版本已经实现同等多源输入和状态化特征逻辑，并通过本地状态函数烟测验证，可作为后续生产级流处理增强。为保证现场演示稳定，提交版默认以 Python 后端完成端到端演示。

== 风险评分与事件入库

评分消费者读取 features.realtime，输出风险判定并写入多个目标：

- risk.scored：完整风险判定事件。
- risk.alerts：高危及严重风险告警。
- risk.audit：评分审计记录。
- PostgreSQL：风险事件、原因码、关联边、审计日志和案件处置记录。
- Redis：高风险节点榜、近期告警队列和团伙风险榜。
- Neo4j：账户转账边、设备关系、IP 关系和商户关系。

每条风险事件包含风险分、风险等级、处置建议、原因码、证据字段、团伙编号和相关节点。大屏点击节点时，后端会返回该节点真实相关的邻居和关联边，而不是仅展示前端当前窗口中的局部节点。

= 可视化大屏

大屏采用 Vue 组件化实现，入口由实时 API 托管。当前页面保留评委需要直接观察的内容，不再放置写给开发者看的冗余说明。主要区域包括：

- 顶部统计：交易总量、风险等级分布、Kafka 吞吐、Flink 估计延迟和模型评分耗时。
- 三维点云图：账户节点按风险强弱改变大小和透明度，高风险节点更醒目，低风险节点淡化。
- 关系线：账户之间的真实关联边以细线展示，支持拖动观察。
- 节点聚焦：点击节点后只展示该节点及其真实相关节点，增强团伙溯源可信度。
- 风险排行：展示高风险账户、风险分、风险等级和团伙编号。
- 事件流：动态展示风险进件和判定结果，避免无限增长导致页面卡顿。
- 解释面板：展示离线先验、实时行为、图团伙和规则命中对最终分数的贡献。
- 审计与案件：展示复核状态、处置动作和审计记录。
- 欺诈剧本命中：统计当前数据流中各类仿真欺诈场景的风险表现。

该大屏不是静态汇报页，而是实时识别工作台。评委可以观察交易流持续进入、风险等级动态变化、节点关系实时更新、告警进入队列、点击节点追溯关系以及审计记录落库。

= 实验结果与验收证据

== 离线模型结果

#table(
  columns: (1.4fr, 1.1fr, 3.4fr),
  [数据集], [可信 AUC], [说明],
  [DGraph-Fin], [0.828120], [核心公开图交易基准，验证图结构、时间统计和树模型融合方案。],
  [DGraph-Fin2], [0.827919], [剔除高风险泄漏字段后仍保持稳定，说明模型不依赖单一异常字段。],
  [IEEE-CIS], [0.914582], [采用时间切分验证，作为表格交易欺诈迁移验证。],
  [EllipticPlusPlus], [0.926556], [采用修正后的可信口径，作为区块链交易图风险识别补充验证。],
)

DGraph-Fin 当前训练集规模为 937584，验证集规模为 104177，训练正样本比例约为 1.27%，特征数为 126。主模型输出提交文件，并保留模型文件、辅助模型文件、指标文件和特征重要度文件。

== 本地烟测结果

DGraph 账户先验烟测 `uv run dgcheater-realtime dgraph-prior-smoke --event-count 80` 已通过。烟测输出模型名为 `dgraph_fin_xgboost_lightgbm_account_prior`，数据集为 DGraph-Fin，特征数为 126，验证 AUC 为 0.828120，样例事件中同时返回源账户和目标账户的 DGraph 模型分。

本地核心烟测 `uv run dgcheater-realtime smoke --event-count 5000` 输出 5000 条事件，其中低危 4615 条、中危 360 条、高危 25 条。样例高危事件能够返回风险分、风险等级、人工复核建议、原因码、DGraph 账户模型先验、实时行为风险、图团伙风险、规则分、渠道、金额、设备账户数、IP 账户数和图邻居数量。

多源汇合烟测 `uv run dgcheater-realtime multisource-join-smoke --event-count 200` 输出 200 条实时特征，待处理事件为 0，200 条均带账户画像历史风险，9 条命中登录挑战，58 条命中黑名单。

Flink 本地状态逻辑烟测 `uv run dgcheater-realtime flink-local-smoke --event-count 200` 输出 200 条清洗交易和 200 条实时特征，证明 Flink 作业中的多源状态函数能够生成完整特征。

大屏契约烟测 `uv run dgcheater-realtime dashboard-contract-smoke --event-count 200` 返回 80 条窗口事件、158 个可见节点、94 条可见边、1 条高危风险事件、20 条中危风险事件，且窗口事件均带原因码。

== 运行库证据

当前本地实时库中已经写入 4441 条风险事件，其中 407 条携带欺诈剧本字段，覆盖商户洗钱、IP 聚集、跨渠道规避、设备复用、试探后转移、分散汇入、突发交易和循环转账等类型。PostgreSQL 中还包含 4630 条风险原因记录、4420 条关联边、5616 条审计记录和 4442 条案件处置记录。Neo4j 中已有 4973 个节点和 8424 条关系，Redis 中维护实时风险缓存。

这些数据说明系统不只是前端模拟展示，而是完成了风险事件入库、审计记录、案件处置和异构图关系写入。

== 性能记录

单机回放性能测试显示，在 100 条交易事件、200 个评分节点的样本上，总运行时间约 0.0304 秒，事件吞吐约 3287.94 笔每秒，节点评分吞吐约 6575.88 个每秒，平均事件端到端延迟约 0.3041 毫秒。该结果用于证明模型推理本身具备低延迟基础，不等同于生产集群压测。

完整 Kafka 与数据库链路的性能由运行指标文件持续记录，包括生产事件数、特征事件数、评分事件数、告警事件数、平均评分耗时和最大评分耗时。当前工程已具备端到端验收脚本，后续可在独立服务器上进行长时间压测。

= 安全合规与可解释性

项目只使用公开匿名数据和仿真字段，不接触真实姓名、证件号、手机号、银行卡号等个人敏感信息。设备、IP 和商户字段均为仿真标识，用于表达风险模式，不指向真实个人或机构。

系统在可解释性上提供三层证据：

- 分数解释：展示 DGraph-Fin 账户模型先验、实时行为、图团伙和规则命中对最终风险分的贡献。
- 原因码解释：输出大额交易、设备共享、IP 聚集、行为突发、黑名单命中、团伙风险等可读原因。
- 图关系解释：保存相关节点和关联边，支持点击节点查看真实邻域和团伙关系。

审计方面，系统会记录风险判定、处置动作和复核状态。评委可以从大屏和数据库同时看到风险产生、原因解释、人工处置和审计留痕。

= 工程交付

当前仓库已经形成以下交付物：

- 离线训练命令和多数据集模型产物。
- 多源仿真器、实时特征后端和风险评分消费者。
- Kafka、PostgreSQL、Redis、Neo4j、Kafka UI、Flink 的 Docker Compose 基础设施。
- Vue 三维实时大屏和 FastAPI 数据接口。
- PostgreSQL 表结构、Redis 实时键和 Neo4j 图模型。
- 本地 JSON Schema 字段契约。
- 端到端验收脚本和多类烟测命令。
- Typst 技术报告和答辩演示稿。

主要启动命令如下：

```powershell
uv run dev-system --feature-backend python
```

该命令会启动基础设施、实时 API、Python 特征后端、评分消费者和仿真交易生产器。若需要尝试 Flink 后端，可使用：

```powershell
uv run dev-system
```

端到端验收命令如下：

```powershell
uv run dgcheater-realtime e2e-check --event-count 200 --timeout-sec 120
```

大屏默认地址为：

```text
http://127.0.0.1:8060
```

= 边界与后续计划

当前项目已经完成可提交的核心闭环，但仍有三类后续增强空间：

- Flink 稳定性：当前 Flink 作业已实现并通过本地状态逻辑验证，但现场稳定演示仍建议使用 Python 特征后端。后续需要继续修复 PyFlink 状态读取在容器环境中的兼容问题，并补充长时间压测。
- 自适应学习：当前已经保留延迟标签和离线再训练链路，后续可加入模型注册、灰度发布、回滚和自动阈值校准。
- 更大规模仿真：当前仿真器已覆盖主要欺诈剧本，后续可接入更完整的 AMLSim 或 IBM AML 数据生成配置，提高交易量级和团伙复杂度。

总体而言，DGCheater 已经从单一离线 AUC 方案升级为可演示、可解释、可审计的实时金融反诈原型。其核心价值在于把图交易识别模型、流式仿真、多源状态特征、风险评分、图关系追溯和风控处置大屏整合为一条清晰闭环，能够支撑方向一作品提交与现场答辩。
