\documentclass[tikz,border=10pt]{standalone}
\usepackage{ctex}
\usepackage{xcolor}
\usepackage{fontspec}
\usepackage{tikz}
\usetikzlibrary{
  arrows.meta,
  calc,
  decorations.pathreplacing,
  fit,
  positioning,
  shadows.blur,
  shapes.geometric,
  shapes.symbols
}

\setmainfont{Times New Roman}
\setCJKmainfont[
  Path = C:/Windows/Fonts/,
  UprightFont = SourceHanSerifSC-Regular.otf,
  BoldFont = SourceHanSerifSC-Bold.otf
]{Source Han Serif SC}

\definecolor{ink}{HTML}{17201F}
\definecolor{muted}{HTML}{61706B}
\definecolor{paper}{HTML}{FAF7EF}
\definecolor{gridline}{HTML}{DDD6C8}
\definecolor{blue}{HTML}{256B8F}
\definecolor{purple}{HTML}{6E57A8}
\definecolor{orange}{HTML}{D96B2B}
\definecolor{green}{HTML}{1F7A62}
\definecolor{gold}{HTML}{A35C1F}
\definecolor{red}{HTML}{B8322A}
\definecolor{teal}{HTML}{0F766E}
\definecolor{softblue}{HTML}{EAF4FA}
\definecolor{softpurple}{HTML}{F1EDFA}
\definecolor{softorange}{HTML}{FFF0E5}
\definecolor{softgreen}{HTML}{EAF6F0}
\definecolor{softgold}{HTML}{FFF4DD}
\definecolor{softred}{HTML}{FDEDEC}
\definecolor{softteal}{HTML}{E7F5F2}

\tikzset{
  font=\small,
  title/.style={font=\LARGE\bfseries,text=ink,align=left},
  subtitle/.style={font=\small,text=muted,align=left},
  panel/.style n args={2}{
    rounded corners=9pt,
    draw=#1!38,
    fill=#2,
    line width=0.7pt,
    inner sep=4pt
  },
  module/.style n args={2}{
    rounded corners=8pt,
    draw=#1!65,
    fill=#2,
    line width=1pt,
    minimum width=39mm,
    minimum height=17mm,
    align=center,
    text=ink,
    blur shadow={shadow blur steps=5,shadow xshift=0.7pt,shadow yshift=-0.7pt,shadow opacity=18}
  },
  wide/.style n args={2}{
    rounded corners=8pt,
    draw=#1!65,
    fill=#2,
    line width=1pt,
    minimum width=52mm,
    minimum height=18mm,
    align=center,
    text=ink,
    blur shadow={shadow blur steps=5,shadow xshift=0.7pt,shadow yshift=-0.7pt,shadow opacity=18}
  },
  submodule/.style n args={2}{
    rounded corners=6pt,
    draw=#1!60,
    fill=#2,
    line width=0.8pt,
    minimum width=30mm,
    minimum height=11mm,
    align=center,
    text=ink
  },
  sourcechip/.style n args={2}{
    rounded corners=5pt,
    draw=#1!60,
    fill=#2,
    line width=0.8pt,
    minimum width=21mm,
    minimum height=9mm,
    align=center,
    text=ink,
    font=\scriptsize
  },
  topic/.style={
    cylinder,
    shape border rotate=90,
    aspect=0.23,
    draw=purple!70,
    fill=softpurple,
    minimum width=34mm,
    minimum height=12mm,
    align=center,
    text=ink,
    line width=0.9pt
  },
  db/.style n args={2}{
    cylinder,
    shape border rotate=90,
    aspect=0.29,
    draw=#1!66,
    fill=#2,
    minimum width=34mm,
    minimum height=14mm,
    align=center,
    text=ink,
    line width=0.9pt
  },
  note/.style={
    rounded corners=5pt,
    draw=gridline,
    fill=paper,
    text=muted,
    font=\scriptsize,
    align=left,
    inner sep=4pt
  },
  script/.style={
    signal,
    signal from=west,
    signal to=east,
    draw=red!72,
    fill=softred,
    minimum width=42mm,
    minimum height=11mm,
    align=center,
    text=ink,
    line width=0.85pt
  },
  flow/.style n args={1}{
    -{Latex[length=2.6mm,width=1.6mm]},
    draw=#1,
    line width=1.25pt
  },
  sideflow/.style n args={1}{
    -{Latex[length=2.2mm,width=1.4mm]},
    draw=#1,
    line width=0.9pt,
    dashed,
    opacity=0.82
  },
  feedback/.style={
    -{Latex[length=2.2mm,width=1.4mm]},
    draw=red,
    line width=0.95pt,
    dotted
  },
  labeltext/.style={
    midway,
    fill=paper,
    inner sep=2.2pt,
    font=\fontsize{7.5pt}{8.5pt}\selectfont,
    text=muted
  },
  flowlabel/.style={
    fill=paper,
    inner sep=2.2pt,
    font=\fontsize{7.5pt}{8.5pt}\selectfont,
    text=muted
  },
  tag/.style n args={1}{
    rounded corners=3pt,
    draw=#1!55,
    fill=#1!10,
    text=#1!72!black,
    font=\scriptsize\bfseries,
    inner xsep=4pt,
    inner ysep=2pt
  }
}

\newcommand{\bul}[1]{\(\triangleright\)~#1}

\begin{document}
\begin{tikzpicture}[x=1mm,y=1mm]
  \fill[paper] (-8,-8) rectangle (422,246);
  \foreach \x in {0,18,...,414} {
    \draw[gridline!32,line width=0.25pt] (\x,-4) -- (\x,240);
  }
  \foreach \y in {0,18,...,234} {
    \draw[gridline!32,line width=0.25pt] (-4,\y) -- (418,\y);
  }

  \node[title,anchor=west] at (0,232) {DGraph 机器学习算法总体链路};
  \node[subtitle,anchor=west] at (0,224) {DGraph-Fin 图数据读取、无泄漏图特征工程、XGBoost 与 LightGBM 融合训练、可信验证、离线模型复用到实时评分};

  \node[panel={blue}{softblue},fit={(2,154) (74,211)},label={[tag={blue},anchor=west]above left:图数据与任务边界}] {};
  \node[panel={orange}{softorange},fit={(88,145) (181,211)},label={[tag={orange},anchor=west]above left:图结构特征工程}] {};
  \node[panel={purple}{softpurple},fit={(195,145) (274,211)},label={[tag={purple},anchor=west]above left:无泄漏监督增强}] {};
  \node[panel={green}{softgreen},fit={(288,145) (405,211)},label={[tag={green},anchor=west]above left:融合模型训练与输出}] {};

  \node[panel={gold}{softgold},fit={(2,62) (145,132)},label={[tag={gold},anchor=west]above left:可信验证与口径控制}] {};
  \node[panel={teal}{softteal},fit={(159,62) (284,132)},label={[tag={teal},anchor=west]above left:实时评分复用}] {};
  \node[panel={red}{softred},fit={(298,62) (405,132)},label={[tag={red},anchor=west]above left:解释与提交产物}] {};

  \node[db={blue}{softblue}] (npz) at (36,196) {dgraphfin.npz};
  \node[sourcechip={blue}{softblue}] at (14,174) {节点特征 \(x\)};
  \node[sourcechip={blue}{softblue}] at (36,174) {边列表};
  \node[sourcechip={blue}{softblue}] at (58,174) {边类型};
  \node[sourcechip={blue}{softblue}] at (14,160) {边时间戳};
  \node[sourcechip={blue}{softblue}] at (36,160) {训练索引};
  \node[sourcechip={blue}{softblue}] at (58,160) {测试索引};

  \node[wide={orange}{softorange}] (graph) at (132,196) {动态图结构展开\\src / dst / type / timestamp};
  \node[submodule={orange}{softorange}] (degree) at (102,169) {入度出度\\log degree};
  \node[submodule={orange}{softorange}] (etype) at (132,169) {边类型计数\\in/out/total};
  \node[submodule={orange}{softorange}] (time) at (162,169) {时间统计\\mean/min/max/span};
  \node[note,text width=79mm] (basefeat) at (132,151) {
    原始 17 维节点特征、缺失值计数、结构统计、时间统计和一跳邻居聚合共同形成基础图特征矩阵。
  };

  \node[wide={purple}{softpurple}] (neighbor) at (234,196) {一跳邻居聚合\\max / min / mean};
  \node[submodule={purple}{softpurple}] (known) at (211,169) {已知标签邻域};
  \node[submodule={purple}{softpurple}] (ratio) at (257,169) {欺诈邻居比例};
  \node[note,text width=64mm] (leak) at (234,151) {
    只使用训练节点标签构造监督邻域特征，验证和测试节点标签不进入特征生成过程。
  };

  \node[module={green}{softgreen}] (xgb) at (313,196) {XGBoost\\主模型};
  \node[module={green}{softgreen}] (lgb) at (371,196) {LightGBM\\辅助模型};
  \node[wide={green}{softgreen}] (blend) at (342,168) {概率融合\\\(p=w_x p_x+w_l p_l\)};
  \node[script] (riskout) at (342,148) {欺诈概率 / 风险排序 / submission};

  \node[module={gold}{softgold}] (split) at (38,114) {训练验证边界\\train / valid / test};
  \node[module={gold}{softgold}] (trust) at (100,114) {泄漏排查\\时间标签禁用};
  \node[note,text width=118mm] at (73,82) {
    \bul{DGraph-Fin 可信验证 AUC 约 0.8281}\quad
    \bul{DGraph-Fin2 移除节点时间标签后约 0.8279}\\
    \bul{特征缓存版本化，训练产物和指标可复跑}
  };

  \node[db={teal}{softteal}] (modelstore) at (189,115) {模型产物\\joblib};
  \node[module={teal}{softteal}] (rtfeat) at (235,115) {实时特征向量\\交易窗口 + 图邻域};
  \node[wide={teal}{softteal}] (rtscore) at (221,84) {离线模型先验接入实时评分\\offline\_model\_score};

  \node[module={red}{softred}] (importance) at (326,115) {特征重要度\\feature\_importance.csv};
  \node[module={red}{softred}] (explain) at (376,115) {评分解释\\reason codes};
  \node[script] (decision) at (351,84) {risk\_score / risk\_level / decision};

  \draw[flow={blue}] (npz.east) -- node[pos=0.48,labeltext,above] {解析图输入} (graph.west);
  \draw[flow={orange}] (graph.south west) -- (degree.north);
  \draw[flow={orange}] (graph.south) -- (etype.north);
  \draw[flow={orange}] (graph.south east) -- (time.north);
  \draw[flow={orange}] (degree.south) -- (basefeat.north west);
  \draw[flow={orange}] (etype.south) -- (basefeat.north);
  \draw[flow={orange}] (time.south) -- (basefeat.north east);
  \draw[flow={orange}] (basefeat.east) -- node[pos=0.5,labeltext,above] {基础特征矩阵} (neighbor.west);

  \draw[flow={purple}] (neighbor.south west) -- (known.north);
  \draw[flow={purple}] (neighbor.south east) -- (ratio.north);
  \draw[flow={purple}] (known.south) -- (leak.north west);
  \draw[flow={purple}] (ratio.south) -- (leak.north east);
  \draw[flow={purple}] (leak.east) -- node[pos=0.48,labeltext,above] {增强特征} (xgb.west);
  \draw[flow={purple}] (leak.east) -- ++(12,0) |- (lgb.west);

  \draw[flow={green}] (xgb.south) -- (blend.north west);
  \draw[flow={green}] (lgb.south) -- (blend.north east);
  \draw[flow={green}] (blend.south) -- (riskout.north);

  \draw[sideflow={gold}] (npz.south) -- (36,142) -- (38,142) -- (split.north);
  \node[flowlabel] at (37,143) {索引边界};
  \draw[sideflow={gold}] (split.east) -- node[pos=0.5,labeltext,above] {验证约束} (trust.west);
  \draw[sideflow={gold}] (trust.north) -- ++(0,23) -- (leak.south);
  \node[flowlabel] at (164,134) {禁用泄漏字段};

  \draw[flow={teal}] (riskout.south west) -- ++(-74,-28) -- (modelstore.north);
  \node[flowlabel] at (235,132) {模型落盘};
  \draw[flow={teal}] (modelstore.east) -- (rtfeat.west);
  \draw[flow={teal}] (rtfeat.south) -- (rtscore.north);
  \draw[flow={teal}] (rtscore.east) -- (decision.west);

  \draw[flow={red}] (importance.east) -- (explain.west);
  \draw[flow={red}] (explain.south) -- (decision.north);

  \draw[decorate,decoration={brace,amplitude=5pt},draw=muted,line width=0.65pt]
    (0,38) -- node[below=6pt,text=muted,font=\scriptsize] {算法闭环：图数据读取 $\rightarrow$ 图统计特征 $\rightarrow$ 无泄漏监督增强 $\rightarrow$ 双模型融合 $\rightarrow$ 可信验证 $\rightarrow$ 实时评分复用} (407,38);

  \node[tag={blue}] at (28,20) {图输入};
  \draw[flow={blue}] (45,20) -- (64,20);
  \node[tag={orange}] at (96,20) {图特征};
  \draw[flow={orange}] (116,20) -- (135,20);
  \node[tag={purple}] at (166,20) {监督增强};
  \draw[flow={purple}] (189,20) -- (208,20);
  \node[tag={green}] at (238,20) {模型融合};
  \draw[flow={green}] (260,20) -- (279,20);
  \node[tag={gold}] at (310,20) {可信验证};
  \draw[sideflow={gold}] (333,20) -- (352,20);
  \node[tag={teal}] at (382,20) {实时复用};
\end{tikzpicture}
\end{document}
